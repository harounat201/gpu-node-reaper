# This rule is not used by the project gpu-node-reaper itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the training.harouna.dev.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: gpu-node-reaper
    app.kubernetes.io/managed-by: kustomize
  name: trainingjob-editor-role
rules:
- apiGroups:
  - training.harouna.dev
  resources:
  - trainingjobs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - training.harouna.dev
  resources:
  - trainingjobs/status
  verbs:
  - get
